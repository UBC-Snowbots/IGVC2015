#include "ros/ros.h"
#include "nav_msgs/OccupancyGrid.h"

// Takes in a local map, and maps it onto a custom local map

class MappingUtilities
{
  private:
  
    int total_width, total_height;
    nav_msgs::OccupancyGrid combined_local_map;
    
    // FUnction for processing vision map
    
  
  public:
  
    // Initializer for combined local map 
    // Mapping function for vision
    // Mapping function for lidar
    // update function for outputting combined local map
     
}
